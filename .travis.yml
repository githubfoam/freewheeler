language: ruby
rvm:
  - 2.5
env:
  packer_version=1.5.1
install:
  - bundle install
  - curl -L -o packer.zip https://releases.hashicorp.com/packer/$packer_version/packer_$packer_version_linux_amd64.zip && unzip -d bin packer.zip
script:
  - PATH=$(pwd)/bin:$PATH bundle exec rake
